{
  "name": "webpack.config",
  "description": "bootstrap repo for creating react app with use of webpack v5",
  "private": true,
  "license": "MIT",
  "version": "1.0.0-alpha",
  "scripts": {
    "build": "cross-env NODE_ENV=production webpack --progress",
    "build:tsc": "cross-env NODE_ENV=production tsc --noEmit --listFiles --listEmittedFiles",
    "build:tsc:config": "cross-env NODE_ENV=production tsc --showConfig",
    "start": "cross-env NODE_ENV=development webpack serve --open --progress",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@tsconfig/node18": "^18.2.2",
    "@types/debug": "^4.1.12",
    "@types/node": "^20.9.1",
    "@types/webpack-dev-server": "^4.7.2",
    "cross-env": "^7.0.3",
    "debug": "^4.3.4",
    "glob": "^10.3.10",
    "ts-node": "^10.9.1",
    "type-fest": "^4.8.1",
    "typescript": "^5.2.2",
    "webpack": "^5.89.0",
    "webpack-cli": "^5.1.4",
    "webpack-dev-server": "^4.15.1"
  },
  "volta": {
    "node": "18.18.2"
  }
}
